// This file runs BEFORE every test file
// It sets up the testing environment

// @testing-library/jest-dom adds custom matchers (assertions)
// for DOM elements. For example:
//
// expect(element).toBeVisible()
// expect(element).toHaveTextContent('Hello')
// expect(element).toBeDisabled()
// expect(button).toHaveClass('active')
//
// Without this, you'd only have generic matchers like:
// expect(value).toBe(true) — much less readable
import '@testing-library/jest-dom';